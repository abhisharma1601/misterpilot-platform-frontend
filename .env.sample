# Backend base URL (used by Next.js route handlers to call the upstream API)
BACKEND_URL=

# Google OAuth
NEXT_PUBLIC_GOOGLE_CLIENT_ID=

# Razorpay (get it from razorpay.com dashboard → Settings → API Keys)
NEXT_PUBLIC_RAZORPAY_KEY_ID=
